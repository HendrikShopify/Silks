{% render 'breadcrumbs' %}
<div class="container section-padding-sm">
  <div class="default-grid">
    <div class="col-span-6 col-start-2">
      <h1 class="h1">
        {{ page.title }}
      </h1>

      <div class="rich-text">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
